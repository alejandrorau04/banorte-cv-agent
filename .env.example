# Copiar a .env y rellenar. .env NUNCA se versiona.

# --- Proveedor LLM (Google AI Studio) ---
GEMINI_API_KEY=

# --- Autenticación del propio endpoint Open Responses ---
# Token que la plataforma del reto debe enviar en Authorization: Bearer <token>
AGENT_API_KEY=

# --- Configuración ---
LOG_LEVEL=INFO
